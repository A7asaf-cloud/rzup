services:
  - type: web
    name: pinkas-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run seed
    startCommand: npm start
    plan: free
    envVars:
      - key: PORT
        value: 4000
